# Optional diagnostics profile. Base MCP rates server wiring now lives in
# application.properties and uses lazy initialization, so backend startup remains
# safe if the server is unavailable.
#
# Keep this profile for ad-hoc override experiments only.
# Example override:
# spring.ai.mcp.client.sse.connections.rates-server.url=http://localhost:8081
