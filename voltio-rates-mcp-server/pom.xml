<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath />
    </parent>

    <groupId>com.voltio</groupId>
    <artifactId>voltio-rates-mcp-server</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>voltio-rates-mcp-server</name>
    <description>
        Standalone MCP server providing the Voltio Agent's GIC (Guaranteed Investment
        Certificate) rate lookup tool (getGicRates), used by the banking-platform
        backend's Savings Insight Chatbot alongside its knowledge-base RAG content to
        answer GIC rate and suitability questions. Also retains the original
        connectivity-proving tool (ping) used by
        backend/.../config/McpClientDiagnostics.java. Deployed independently of the
        banking-platform backend, which connects to it over MCP (see
        spring.ai.mcp.client.sse.connections.rates-server in application.properties)
        and degrades gracefully if it is unreachable.
    </description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <spring-ai.version>2.0.0</spring-ai.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- MCP server autoconfiguration over Spring MVC (SSE transport). Pulls in
             spring-boot-starter-web transitively. -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>21</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
